
describe( 'Compression tests', () => {
	it.skip( 'Basic compression', () => {
	} );
	
	it.skip( 'Schema with 254 keys uses 8 bit index', () => {
	} );
	
	it.skip( 'Schema with 255 keys uses 16 bit index', () => {
	} );
	
	it.skip( 'Schema with 65353 keys uses 16 bit index', () => {
	} );
	
	it.skip( 'Schema with 65354 keys uses 32 bit index', () => {
	} );
	
	it.skip( 'String is encoded using utf8', () => {
	} );
	
	it.skip( 'String is encoded using utf16', () => {
	} );
	
	it.skip( 'String is encoded using [some other string encoding]', () => {
	} );
	
	it.skip( 'Integer with min of 0 and max value of 255 uses 8 bits', () => {
	} );
	
	it.skip( 'Integer with min of 0 and max value of 65355 uses 16 bits', () => {
	} );
} );
