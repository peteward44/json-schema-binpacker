const { assert } from 'chai';
const binPacker = require( '..' );

describe( 'basic', () => {
	it.skip( 'compress', () => {
		const schema = {
			
		};
		
		const object = {
			
		};
		
		const buffer = binPacker.compress( schema, object );
		
	} );
	
	it.skip( 'decompress', () => {
		
	} );
	
	it.skip( 'compress and decompress', () => {
		
	} );
	
	it.skip( 'compress and decompress with different schema will fail', () => {
		
	} );
} );
